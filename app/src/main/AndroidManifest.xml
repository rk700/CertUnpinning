<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="lab.galaxy.certunpinning">

    <application
        android:hasCode="false"
		android:label="CertUnpinning">
        <meta-data
            android:name="yahfa.hook.plugin"
            android:value="true"
        />
    </application>

</manifest>
